<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="PraktikaShop.BasketWindow"
		x:CompileBindings="False"
        Title="Корзина">
	<DockPanel>
		<StackPanel DockPanel.Dock="Top" Margin="5" Orientation="Horizontal" HorizontalAlignment="Center">
			<TextBlock Text="Добро пожаловать в корзину"/>
		</StackPanel>

		<StackPanel DockPanel.Dock="Bottom" Margin="5" Orientation="Horizontal" HorizontalAlignment="Center">
			<Button Content="Вернуться к списку товаров" Margin="5"/>
			<Button Content="Создать заказ" Margin="5"/>
		</StackPanel>
<!--
		<ListBox>
			<ListBox.ItemsPanel>
				<DataTemplate>
					<Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" Padding="10">
						<DockPanel>
							<Image Margin="5" Source="{Binding ParseImage}" DockPanel.Dock="Left" VerticalAlignment="Top" />
							<StackPanel Margin="5" VerticalAlignment="Center">
								<TextBlock Text="{Binding ProductName, StringFormat='Название товара: {0}'}"
                                               FontWeight="Bold"/>
								<TextBlock Text="{Binding Cost, StringFormat='Стоимость: {0} руб.'}"
										   Margin="0,5,0,0"/>
								<TextBlock Text="{Binding Count, StringFormat='Осталось товара: {0} шт.'}"
										   Margin="0,5,0,0"/>
							</StackPanel>
						</DockPanel>						
					</Border>
				</DataTemplate>
			</ListBox.ItemsPanel>
		</ListBox> -->
		
	</DockPanel>
</Window>
